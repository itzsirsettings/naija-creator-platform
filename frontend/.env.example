# Local development (Next.js)
# Leave NEXT_PUBLIC_API_URL blank so the browser calls same-origin "/api",
# which next.config.mjs rewrites to BACKEND_URL (no CORS, cookies work).
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_DEMO_FALLBACK=false
NEXT_PUBLIC_SENTRY_DSN=
BACKEND_URL=http://localhost:5000
